<script type="text/javascript">
  $(function() {
    var myConfig = { dompath: true };
    <% ids.each do |id| %>
      tinyMCE.init(<%= raw TinyMCE::Rails.configuration.options.merge({ :selector => "##{id}" }).to_json %>);
    <% end %>
  });
</script>
